version: "3.9"

services:
  # ─── PostgreSQL ─────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: codon
      POSTGRES_USER: codon
      POSTGRES_PASSWORD: codon
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U codon"]
      interval: 5s
      timeout: 5s
      retries: 10

  # ─── Redis ──────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ─── API Server ─────────────────────────────────────────────────────────────
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: /bin/api
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      ENV: development
      DATABASE_URL: postgres://codon:codon@postgres:5432/codon?sslmode=disable
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
      JWT_EXPIRY_DAYS: "90"
      TWO_FACTOR_API_KEY: ${TWO_FACTOR_API_KEY:-}
      RAZORPAY_KEY_ID: ${RAZORPAY_KEY_ID:-}
      RAZORPAY_KEY_SECRET: ${RAZORPAY_KEY_SECRET:-}
      RAZORPAY_WEBHOOK_SECRET: ${RAZORPAY_WEBHOOK_SECRET:-}
      S3_ENDPOINT: ${S3_ENDPOINT:-}
      S3_REGION: ${S3_REGION:-us-east-1}
      S3_BUCKET: ${S3_BUCKET:-codon}
      S3_ACCESS_KEY_ID: ${S3_ACCESS_KEY_ID:-}
      S3_SECRET_ACCESS_KEY: ${S3_SECRET_ACCESS_KEY:-}
      OTP_RATE_LIMIT_PER_HOUR: "3"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  # ─── Background Worker ──────────────────────────────────────────────────────
  # worker:
  #   build:
  #     context: ..
  #     dockerfile: docker/Dockerfile
  #   command: /bin/worker
  #   environment:
  #     DATABASE_URL: postgres://codon:codon@postgres:5432/codon?sslmode=disable
  #     REDIS_URL: redis://redis:6379
  #     WORKER_POLL_SECONDS: "5"
  #     S3_ENDPOINT: ${S3_ENDPOINT:-}
  #     S3_REGION: ${S3_REGION:-us-east-1}
  #     S3_BUCKET: ${S3_BUCKET:-codon}
  #     S3_ACCESS_KEY_ID: ${S3_ACCESS_KEY_ID:-}
  #     S3_SECRET_ACCESS_KEY: ${S3_SECRET_ACCESS_KEY:-}
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   restart: unless-stopped

volumes:
  postgres_data:
